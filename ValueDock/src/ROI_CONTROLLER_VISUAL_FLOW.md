# ROI Controller - Visual Flow Diagram

## 🎨 Complete Execution Flow

### Before (Multi-Pass with Dummy Data) ❌

```
┌─────────────────────────────────────────────────────────────┐
│                      USER LOGS IN                           │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  App Initializes                                            │
│  - inputData.processes = []  (empty)                        │
│  - inputData.groups = []     (empty)                        │
│  - selectedProcessIds = []   (empty)                        │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  ROI useMemo Fires #1                                       │
│  ❌ Calculates with DUMMY DATA                              │
│  Result: annualNetSavings = 0                               │
│  Status: "Using default classification"                     │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  Backend Data Loads                                         │
│  - 5 processes loaded                                       │
│  - 3 groups loaded                                          │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  Auto-Select Effect Fires                                   │
│  - selectedProcessIds = [id1, id2, ...]                     │
│  - setDataReadyForROI(true)                                 │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  ROI useMemo Fires #2                                       │
│  ❌ Calculates with "none" classification                   │
│  Result: annualNetSavings = 100000                          │
│  Status: "Using default classification"                     │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  ROI useMemo Fires #3                                       │
│  ❌ Calculates with "default" classification                │
│  Result: annualNetSavings = 120000                          │
│  Status: "Using default cost classification"                │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  Cost Classification Loads                                  │
│  - hardCosts: [Labor, Benefits, Software]                   │
│  - softCosts: [Opportunity Cost, Quality, Speed]            │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  ROI useMemo Fires #4                                       │
│  ✅ Calculates with "custom" classification                 │
│  Result: annualNetSavings = 150000                          │
│  Status: "CUSTOM (loaded from backend)"                     │
└─────────────────────────────────────────────────────────────┘

TOTAL: 4 ROI calculations (3 wrong, 1 correct)
```

---

### After (Single-Pass with Controller) ✅

```
┌─────────────────────────────────────────────────────────────┐
│                      USER LOGS IN                           │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  App Initializes                                            │
│  - inputData.processes = []  (empty)                        │
│  - inputData.groups = []     (empty)                        │
│  - selectedProcessIds = []   (empty)                        │
│  - dataReadyForROI = false                                  │
│  - costClassificationLoaded = false                         │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  ROI useEffect Fires                                        │
│  🚫 BLOCKED: dataReady=false, clsReady=false                │
│  Controller: "Not ready yet"                                │
│  NO CALCULATION                                             │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  loadDataForCurrentContext()                                │
│  - resetROIController() 🔄                                  │
│  - setCostClassificationLoaded(false)                       │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  Backend Data Loads                                         │
│  - 5 processes loaded (mustArray validates)                 │
│  - 3 groups loaded (mustArray validates)                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  Auto-Select Effect Fires (with debounce)                   │
│  - Waits 0ms (next tick)                                    │
│  - selectedProcessIds = [id1, id2, ...] (mustArray)         │
│  - setDataReadyForROI(true) ✓                               │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  ROI useEffect Fires                                        │
│  🚫 BLOCKED: dataReady=true, clsReady=false                 │
│  Controller: "Cost classification not loaded"               │
│  NO CALCULATION                                             │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  Cost Classification Loads                                  │
│  - hardCosts: [Labor, Benefits, Software]                   │
│  - softCosts: [Opportunity Cost, Quality, Speed]            │
│  - setCostClassificationLoaded(true) ✓                      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  ROI useEffect Fires                                        │
│  ✅ READY: dataReady=true, clsReady=true                    │
│  Controller: scheduleROI("data or classification changed")  │
│                                                             │
│  Controller Checks:                                         │
│  ✓ readyForROI = true                                       │
│  ✓ running = false                                          │
│  ✓ timeSinceLastRun > 200ms                                 │
│                                                             │
│  🎯 RUN CALCULATION                                         │
│  - processCount: 5                                          │
│  - selectedCount: 5                                         │
│  - costClassification: CUSTOM                               │
│  - Result: annualNetSavings = 150000                        │
│  - ✅ COMPLETE                                              │
└─────────────────────────────────────────────────────────────┘

TOTAL: 1 ROI calculation (correct from the start)
```

---

## 🔍 Controller State Machine

```
┌──────────────────┐
│  NOT READY       │
│  readyForROI:    │◄──────┐
│  false           │       │
└────────┬─────────┘       │
         │                 │
         │ Data Loads      │ Reset
         │ Classification  │
         │ Loads           │
         ↓                 │
┌──────────────────┐       │
│  READY           │       │
│  readyForROI:    │       │
│  true            │       │
│  running: false  │       │
└────────┬─────────┘       │
         │                 │
         │ scheduleROI()   │
         │                 │
         ↓                 │
┌──────────────────┐       │
│  RUNNING         │       │
│  readyForROI:    │       │
│  true            │       │
│  running: true   │───────┤
└────────┬─────────┘       │
         │                 │
         │ Calculation     │
         │ Completes       │
         ↓                 │
┌──────────────────┐       │
│  COMPLETE        │       │
│  readyForROI:    │       │
│  true            │       │
│  running: false  │       │
│  lastRunAt: now()│───────┘
└──────────────────┘
         │
         │ Org Switch /
         │ Data Change
         └───────────────────────┐
                                 ↓
                         resetROIController()
```

---

## 🎯 Validation Flow

```
┌─────────────────────────────────────────────────────────────┐
│  Backend API Response                                       │
│  {                                                          │
│    data: {                                                  │
│      processes: [...],  ← MUST be array                     │
│      groups: [...]      ← MUST be array                     │
│    }                                                        │
│  }                                                          │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  mustArray Validation                                       │
│                                                             │
│  const groups = mustArray('API.groups', response.groups);   │
│  const processes = mustArray('API.processes', resp.procs);  │
│                                                             │
│  IF Array.isArray(value):                                  │
│    ✅ return value                                          │
│  ELSE:                                                      │
│    ❌ throw Error("expected array, got ...")                │
└─────────────────────────────────────────────────────────────┘
                            ↓
                    ┌───────┴───────┐
                    │               │
                   ✅              ❌
                ARRAY            ERROR
                    │               │
                    ↓               ↓
        ┌───────────────┐   ┌─────────────┐
        │ Continue      │   │ App Crashes │
        │ Processing    │   │ (Good!)     │
        │               │   │             │
        │ Set State     │   │ Shows Error │
        │ Calculate ROI │   │ Message     │
        └───────────────┘   └─────────────┘
```

---

## 📊 Comparison Chart

| Feature | Before (useMemo) | After (Controller) |
|---------|------------------|-------------------|
| **Executions per load** | 3-4 times | 1 time |
| **Dummy data shown** | Yes ❌ | No ✅ |
| **Error handling** | Silent fallbacks ❌ | Throws errors ✅ |
| **Debouncing** | No ❌ | Yes (200ms) ✅ |
| **State tracking** | No ❌ | Yes (running flag) ✅ |
| **Reset on context** | No ❌ | Yes ✅ |
| **Console logging** | Basic ❌ | Detailed with emojis ✅ |
| **Debugging** | Hard ❌ | Easy ✅ |

---

## 🔧 Troubleshooting Decision Tree

```
                    ROI Not Calculating?
                            ↓
          ┌─────────────────┴─────────────────┐
          │                                   │
    Check Console                      Check State
          ↓                                   ↓
    🚫 BLOCKED?                      dataReadyForROI?
          ↓                                   ↓
    ┌─────┴─────┐                      ┌─────┴─────┐
    │           │                      │           │
 dataReady?  clsReady?           true?        false?
    │           │                      │           │
    ↓           ↓                      ↓           ↓
  Check       Check              Ready to     Not ready
processes   classification         run           yet
  loaded      loaded                              │
                                                  ↓
                                          Wait for data
                                          to load

                Multiple Calculations?
                            ↓
                  Search: "🎯 RUN"
                            ↓
                      Count > 1?
                            ↓
          ┌─────────────────┴─────────────────┐
          │                                   │
    Debounce                             Duplicate
    failing?                             effect?
          ↓                                   ↓
    Check                               Check
  MIN_RE_RUN_MS                        useEffect
    = 200ms                            dependencies

                Dummy Data Showing?
                            ↓
                  Search: "ensureArray"
                            ↓
                      Found matches?
                            ↓
          ┌─────────────────┴─────────────────┐
          │                                   │
         Yes                                 No
          ↓                                   ↓
    Replace with                        Check backend
    mustArray                           response
          ↓                                   ↓
    Errors will                         Validate
    surface now                         returns arrays
```

---

## 🎉 Success Indicators

### Console Pattern (Look for this):

```
Step 1: Initial Block
🚫 BLOCKED { dataReady: false, clsReady: false }
                    ↓
Step 2: Data Loads
✅ Data merged with defaults
                    ↓
Step 3: Still Blocked
🚫 BLOCKED { dataReady: true, clsReady: false }
                    ↓
Step 4: Classification Loads
✅ Cost classification loaded
                    ↓
Step 5: Single Run
🎯 RUN { status: "CUSTOM (loaded from backend)" }
                    ↓
Step 6: Complete
✅ COMPLETE { annualNetSavings: 150000 }
```

### Visual UI Flow:

```
┌─────────────┐    ┌──────────────┐    ┌─────────────┐
│  Loading    │ →  │  Loading     │ →  │  Data       │
│  Spinner    │    │  Spinner     │    │  Displayed  │
│             │    │  (with data) │    │             │
│ No Data     │    │ No ROI yet   │    │ Final ROI   │
└─────────────┘    └──────────────┘    └─────────────┘
 dataReady=false    dataReady=true      Both=true
 clsReady=false     clsReady=false      🎯 RUN
```

---

## 📝 Quick Reference

### Key Functions:

| Function | Purpose | When to Use |
|----------|---------|-------------|
| `isROIReady()` | Check if ready | Before scheduling |
| `scheduleROI()` | Trigger calculation | Only way to run ROI |
| `resetROIController()` | Clear state | Org switch, logout |
| `mustArray()` | Validate arrays | All state boundaries |

### Key Flags:

| Flag | Meaning | Set When |
|------|---------|----------|
| `dataReadyForROI` | Data loaded | Auto-select completes |
| `costClassificationLoaded` | Classification ready | Backend fetch done |
| `readyForROI` | Both flags true | Ready to calculate |
| `running` | Calculation in progress | During ROI execution |

---

This visual guide shows the complete transformation from multi-pass with dummy data to single-pass with validation!
